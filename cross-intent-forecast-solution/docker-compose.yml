services:
  trainer:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env.example
    volumes:
      - ./:/workspace
    working_dir: /workspace
    command: python scripts/train.py
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
